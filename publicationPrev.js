function downloadPDF() {
    // URL of the PDF file
    var pdfURL = '/Ppdf/Artvocacy.pdf';
    
    // Create an anchor element
    var link = document.createElement('a');
    
    // Set the href attribute to the PDF URL
    link.href = pdfURL;
    
    // Set the download attribute to force download
    link.download = 'Artvocacy.pdf';
    
    // Programmatically click the link to trigger the download
    link.click();
}

function downloadPDFPolicyBrief() {
    // URL of the PDF file
    var pdfURL = '/Ppdf/YPB.pdf';
    
    // Create an anchor element
    var link = document.createElement('a');
    
    // Set the href attribute to the PDF URL
    link.href = pdfURL;
    
    // Set the download attribute to force download
    link.download = 'YPB.pdf';
    
    // Programmatically click the link to trigger the download
    link.click();
}


function downloadPDFWAP() {
    // URL of the PDF file
    var pdfURL = '/Ppdf/WAP.pdf';
    
    // Create an anchor element
    var link = document.createElement('a');
    
    // Set the href attribute to the PDF URL
    link.href = pdfURL;
    
    // Set the download attribute to force download
    link.download = 'WAP.pdf';
    
    // Programmatically click the link to trigger the download
    link.click();
}

function downloadPDFRelease() {
    // URL of the PDF file
    var pdfURL = '/Ppdf/AONDL.pdf';
    
    // Create an anchor element
    var link = document.createElement('a');
    
    // Set the href attribute to the PDF URL
    link.href = pdfURL;
    
    // Set the download attribute to force download
    link.download = 'AONDL.pdf';
    
    // Programmatically click the link to trigger the download
    link.click();
}

function downloadPDFADOI() {
    // URL of the PDF file
    var pdfURL = '/Ppdf/ADOI.pdf';
    
    // Create an anchor element
    var link = document.createElement('a');
    
    // Set the href attribute to the PDF URL
    link.href = pdfURL;
    
    // Set the download attribute to force download
    link.download = 'ADOI.pdf';
    
    // Programmatically click the link to trigger the download
    link.click();
}

function downloadPDFHMP() {
    // URL of the PDF file
    var pdfURL = '/Ppdf/HMP.pdf';
    
    // Create an anchor element
    var link = document.createElement('a');
    
    // Set the href attribute to the PDF URL
    link.href = pdfURL;
    
    // Set the download attribute to force download
    link.download = 'HMP.pdf';
    
    // Programmatically click the link to trigger the download
    link.click();
}

function downloadPDFPVM() {
    // URL of the PDF file
    var pdfURL = '/Ppdf/PVM.pdf';
    
    // Create an anchor element
    var link = document.createElement('a');
    
    // Set the href attribute to the PDF URL
    link.href = pdfURL;
    
    // Set the download attribute to force download
    link.download = 'PVM.pdf';
    
    // Programmatically click the link to trigger the download
    link.click();
}

function downloadPDFrEc() {
    // URL of the PDF file
    var pdfURL = '/Ppdf/REC.pdf';
    
    // Create an anchor element
    var link = document.createElement('a');
    
    // Set the href attribute to the PDF URL
    link.href = pdfURL;
    
    // Set the download attribute to force download
    link.download = 'REC.pdf';
    
    // Programmatically click the link to trigger the download
    link.click();
}

function downloadPDFWEB() {
    // URL of the PDF file
    var pdfURL = '/Ppdf/WEB.pdf';
    
    // Create an anchor element
    var link = document.createElement('a');
    
    // Set the href attribute to the PDF URL
    link.href = pdfURL;
    
    // Set the download attribute to force download
    link.download = 'WEB.pdf';
    
    // Programmatically click the link to trigger the download
    link.click();
}

function downloadPDFUGS() {
    // URL of the PDF file
    var pdfURL = '/Ppdf/UGS.pdf';
    
    // Create an anchor element
    var link = document.createElement('a');
    
    // Set the href attribute to the PDF URL
    link.href = pdfURL;
    
    // Set the download attribute to force download
    link.download = 'UGS.pdf';
    
    // Programmatically click the link to trigger the download
    link.click();
}

function downloadCPPS() {
    // URL of the PDF file
    var pdfURL = '/Ppdf/PPC.pdf';
    
    // Create an anchor element
    var link = document.createElement('a');
    
    // Set the href attribute to the PDF URL
    link.href = pdfURL;
    
    // Set the download attribute to force download
    link.download = 'PPC.pdf';
    
    // Programmatically click the link to trigger the download
    link.click();
}

function downloadHRB() {
    // URL of the PDF file
    var pdfURL = '/Ppdf/hrb.pdf';
    
    // Create an anchor element
    var link = document.createElement('a');
    
    // Set the href attribute to the PDF URL
    link.href = pdfURL;
    
    // Set the download attribute to force download
    link.download = 'hrb.pdf';
    
    // Programmatically click the link to trigger the download
    link.click();
}

function downloadSOP() {
    // URL of the PDF file
    var pdfURL = '/Ppdf/sop.pdf';
    
    // Create an anchor element
    var link = document.createElement('a');
    
    // Set the href attribute to the PDF URL
    link.href = pdfURL;
    
    // Set the download attribute to force download
    link.download = 'sop.pdf';
    
    // Programmatically click the link to trigger the download
    link.click();
}

function downloadCSO() {
    // URL of the PDF file
    var pdfURL = '/Ppdf/cso.pdf';
    
    // Create an anchor element
    var link = document.createElement('a');
    
    // Set the href attribute to the PDF URL
    link.href = pdfURL;
    
    // Set the download attribute to force download
    link.download = 'cso.pdf';
    
    // Programmatically click the link to trigger the download
    link.click();
}

function downloadNIDDLE() {
    // URL of the PDF file
    var pdfURL = '/Ppdf/Needle.pdf';
    
    // Create an anchor element
    var link = document.createElement('a');
    
    // Set the href attribute to the PDF URL
    link.href = pdfURL;
    
    // Set the download attribute to force download
    link.download = 'Needle.pdf';
    
    // Programmatically click the link to trigger the download
    link.click();
}

function downloadOH() {
    // URL of the PDF file
    var pdfURL = '/Ppdf/orangeHands.pdf';
    
    // Create an anchor element
    var link = document.createElement('a');
    
    // Set the href attribute to the PDF URL
    link.href = pdfURL;
    
    // Set the download attribute to force download
    link.download = 'orangeHands.pdf';
    
    // Programmatically click the link to trigger the download
    link.click();
}

function downloadSixteenDays() {
    // URL of the PDF file
    var pdfURL = '/Ppdf/16Days2022.pdf';
    
    // Create an anchor element
    var link = document.createElement('a');
    
    // Set the href attribute to the PDF URL
    link.href = pdfURL;
    
    // Set the download attribute to force download
    link.download = '16Days2022.pdf';
    
    // Programmatically click the link to trigger the download
    link.click();
}